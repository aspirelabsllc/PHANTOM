import { Threshold } from "@/components/threshold/threshold";

// The public entrance. Middleware sends authenticated invokers to /gallery.
export default function ThresholdPage() {
  return <Threshold />;
}
